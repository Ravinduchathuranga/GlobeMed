package com.rc008code.hms.mediator.api;

/**
 * Base colleague interface for components that participate in appointment scheduling.
 */
public interface AppointmentColleague {
    void setMediator(AppointmentMediator mediator);
}
